@model Teacher

@{
    ViewData["Title"] = "Xóa giảng viên";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}
<div class="card">
    <h2 class="card-header">Xóa giảng viên</h2>
    <div class="container-form">
        <p>Bạn có chắc chắn muốn xóa giảng viên viên "@Model.Name" không?</p>
        <form method="post" asp-action="Delete" asp-controller="teacher" asp-route-mid="@Model.TeacherId">
            <input type="hidden" asp-for="@Model.TeacherId" />
            <div class="form-group">
                <input type="submit" class="btn btn-danger" value="Delete" />
                <a class="btn btn-secondary" asp-action="Index" >Quay lại</a>
            </div>

        </form>
    </div>
</div>
